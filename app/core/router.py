"""Core API router — placeholder, no endpoints defined yet.

Health check, config introspection, and other infra-level endpoints
can be added here when needed.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/core", tags=["core"])
